import React from "react";
import { Provider } from "react-redux";
import { Store } from "redux";
import { BoxBorderColorInput } from "./BoxBorderColorInput";

test("1 is 1", () => {
  expect(1).toBe(1);
});

describe("BoxBorderColor", () => {
  const state = {
    color: "#000000"
  };
  it("BorderColor Change", () => {});
});
